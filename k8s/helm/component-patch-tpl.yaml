apiVersion: v1
values:
  images:
    componentOperator: cloudogu/k8s-component-operator:1.3.0
    kubeRbacProxy: gcr.io/kubebuilder/kube-rbac-proxy:v0.14.1
patches:
  values.yaml:
    additionalImages:
    kubeRbacProxy:
      image:
        registry: "{{ registryFrom .images.kubeRbacProxy }}"
        repository: "{{ repositoryFrom .images.kubeRbacProxy }}"
        tag: "{{ tagFrom .images.kubeRbacProxy }}"
    manager:
      image:
        registry: "{{ registryFrom .images.componentOperator }}"
        repository: "{{ repositoryFrom .images.componentOperator }}"
        tag: "{{ tagFrom .images.componentOperator }}"
