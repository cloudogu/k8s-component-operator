apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: metrics-reader
  labels:
    app: ces
    app.kubernetes.io/name: k8s-component-operator
rules:
  - nonResourceURLs:
      - "/metrics"
    verbs:
      - get